#firstpart

a=c(103,82,179)

for(val in a)
{
  if(val %% 2 == 0)
  
    print("Not Prime")

  else(print("Prime"))  
}

#secondpart

b <- c("above","unit","under")

grep(pattern = "a",b,value = T)
grep(pattern = "u",b,value = T)
